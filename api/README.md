# Secrets service

Secrets service implements Secrets [API](../API.md).

## Tests

Install dependencies:

    $ npm install

Start Vault:

    $ make vault-test-setup

Start server:

    $ npm start

Run Jest tests:

    $ npm test
