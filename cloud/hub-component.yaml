---
version: 1
kind: component
meta:
  name: secrets-service-cloud
  source:
    dir: .

parameters:
- name: terraform.bucket.name
  env: STATE_BUCKET
- name: terraform.bucket.region
  env: STATE_REGION
- name: dns.name
  default: default
  env: TF_VAR_name
- name: dns.baseDomain
  default: stacks.delivery
  env: TF_VAR_base_domain
- name: cloud.region
  kind: user
  env: AWS_DEFAULT_REGION
- name: component.secrets-service.name
  default: secrets-service
  env: COMPONENT_NAME

outputs:
- name: component.secrets-service.image
  brief: Agile Stacks secret service  docker repository
  fromTfVar: repository_url
